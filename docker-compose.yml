version: '3'
services:
  elasticsearch:
    container_name: elasticsearch
    image: elasticsearch:7.14.2
    environment:
      - node.name=elasticsearch
      - discovery.type=single-node
      - cluster.name=docker-cluster
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - esdata:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
      - "9300:9300"
    networks:
      - esnet
  app_dev:
    container_name: docker-spring-boot-dev
    profiles: ["dev"]
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - elasticsearch
    networks:
      - esnet
    restart: on-failure
  app_test:
    container_name: docker-spring-boot-test
    profiles: ["test"]
    environment:
      - SPRING_PROFILES_ACTIVE=test
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - elasticsearch
    networks:
      - esnet
    restart: on-failure
networks:
  esnet:
    driver: bridge
volumes:
  esdata:
    driver: local
